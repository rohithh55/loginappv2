pipeline {
    agent any
    environment {
        
   }

    stages {
        stage('Build') {
            steps {
                echo 'Building..'
                sh 'cd loginappv2 && npm install && npm run build'
            }
        }
       
    }
}
